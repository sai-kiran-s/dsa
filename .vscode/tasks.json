{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "compile",
        "type": "shell",
        "command": "javac",
        "args": [
          "${file}"
        ],
        "group": {
          "kind": "build",
          "isDefault": false
        }
      },
      {
        "label": "compile and run",
        "type": "shell",
        "command": "javac \"${file}\" && java $(pkg=$(grep -m1 '^package ' \"${file}\" | sed 's/package //;s/;//'); if [ -n \"$pkg\" ]; then echo \"$pkg.\"; fi)$(basename \"${file}\" .java) < /Users/saikirans/dsa/input.txt > /Users/saikirans/dsa/output.txt",
        "group": {
          "kind": "build",
          "isDefault": true
        }
      }
    ]
  }